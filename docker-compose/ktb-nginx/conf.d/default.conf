upstream portainer_upstream {
    server ktb-portainer:9000;
}

upstream registry_upstream {
    server ktb-private-registry:5000;
}

upstream frontend_upstream {
    server 10.0.1.223:3000;
}

upstream backend_upstream {
    server 10.0.1.119:8080;
}

server {
    listen 80;
    server_name wepick.cloud portainer.wepick.cloud;
    
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name wepick.cloud;
    server_tokens off;

    ssl_certificate /etc/letsencrypt/live/wepick.cloud-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/wepick.cloud-0001/privkey.pem;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://frontend_upstream;
    }
}

server {
    listen 443 ssl;
    server_name portainer.wepick.cloud;
    server_tokens off;

    ssl_certificate /etc/letsencrypt/live/wepick.cloud-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/wepick.cloud-0001/privkey.pem;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://portainer_upstream;
    }
}

server {
    listen 443 ssl;
    server_name registry.wepick.cloud;
    server_tokens off;

    ssl_certificate /etc/letsencrypt/live/wepick.cloud-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/wepick.cloud-0001/privkey.pem;

    # image push 시 큰 용량의 파일 업로드를 위해 최대 허용 크기 증가
    client_max_body_size 1g;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://registry_upstream;
    }
}
